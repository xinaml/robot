package ${dir}.ser.${package};

import ${dir}.base.service.ServiceImpl;
import ${dir}.dto.${package}.${className}DTO;
import ${dir}.entity.${package}.${className};
import org.springframework.stereotype.Service;

/**
 * ${des}业务接口实现
 *
 * @Author:	[${author}]
 * @Date: [${date(),dateFormat="yyyy-MM-dd HH:mm:ss"}]
 * @Description: [${des}]
 * @Version: [${version}]
 * @Copy: [${dir}]
 */
@Service
public class ${className}SerImp extends ServiceImpl<${className}, ${className}DTO> implements ${className}Ser {
}
