package ${dir}.ser.${package};

import ${dir}.base.service.Ser;
import ${dir}.entity.${package}.${className};
import ${dir}.dto.${package}.${className}DTO;

/**
 * ${des}业务接口
 *
 * @Author:	[${author}]
 * @Date: [${date(),dateFormat="yyyy-MM-dd HH:mm:ss"}]
 * @Description: [${des}]
 * @Version: [${version}]
 * @Copy: [${dir}]
 */
public interface ${className}Ser extends Ser<${className}, ${className}DTO> {
}
